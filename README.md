Installation guide:
pip install -r requirements.txt - installs all needed dependencies for the project

Usage guide:
main.ipynb demonstrates all features of our project, it was created for demonstration purposes. Every .py file is used as a module and can be called in various situtatons, thus achieving code reusability.